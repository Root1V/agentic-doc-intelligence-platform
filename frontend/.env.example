# Minutos que, por asunción explícita y configurable, tomaría procesar un
# documento manualmente sin la plataforma — usado solo para estimar el
# "tiempo ahorrado" en el dashboard. Ajustar según el proceso manual real
# que se esté reemplazando; no es un dato medido, es un supuesto declarado.
VITE_ASSUMED_MINUTES_PER_DOCUMENT=8
